Allgemeines Vorgehen:
Entity anlegen (construct ohne id)
(Interface Repository anlegen) ?
api java class anlegen
Rest Controller anlegen (kann mit Repository kommunizieren/ Funktionen nutzen)
Service anlegen (erstellt neue Objekte)

IRepository -> Service -> RestController


Bugs to fix:
birthday java sql date wird als int value gespeichert?

General tasks:
enum gender adden
birthday validierung